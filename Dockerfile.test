FROM golang:1.25-alpine

RUN apk add --no-cache ca-certificates tzdata

WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY *.go ./
COPY migrations/ ./migrations/
COPY scripts/ ./scripts/
COPY skills/ ./skills/

CMD ["go", "test", "-count=1", "-timeout", "60s", "-v", "."]
